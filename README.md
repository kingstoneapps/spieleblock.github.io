# SpieleblockWebsite

Dieses Repository enthält den Code für die mit GitHub Pages erstellte Website zur Android App *Spieleblock*.

## Lokal testen (optional)

```bash
bundle install
bundle exec jekyll serve
```
